<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.enation.app.javashop.mapper.promotion.minus.MinusMapper">

    <!-- 单品立减分页查询 -->
    <select id="selectMinusVoPage" resultType="com.enation.app.javashop.model.promotion.minus.vo.MinusVO" useCache="false">
        SELECT * FROM es_minus ${ew.customSqlSegment}
    </select>
</mapper>
